[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rhm-llm-prompts"
version = "0.1.0"
description = "Versioned LLM prompt templates for Rabbit Hole Mapper"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["rhm_llm_prompts*"]
